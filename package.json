{
  "name": "m3quiz",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source Japanese reasoning quiz and question bank",
  "type": "module",
  "scripts": {
    "dev": "npx --yes http-server . -p 8080 -c-1",
    "validate": "node scripts/validate-data.mjs",
    "report:data": "node scripts/data-quality-report.mjs",
    "check": "npm run validate && npm run report:data && npm test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
